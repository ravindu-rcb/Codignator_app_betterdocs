<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Send everything except real files and dirs to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
